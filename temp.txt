public class LocaleDemo {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//testing different zones		
		//ZoneId.getAvailableZoneIds().stream().forEach(System.out::println);
		System.out.println(ZoneId.getAvailableZoneIds().stream().distinct().count());	
		
		//Arrays.asList(Locale.getAvailableLocales()).stream().forEach(System.out::println);
		//currency
		float amount=76000f;
		for(Locale locale :Locale.getAvailableLocales())
		{
		NumberFormat numberFormatter=NumberFormat.getCurrencyInstance(locale);
		String moneyFormatter=numberFormatter.format(amount);
		System.out.println(moneyFormatter);
		}
		/*
		for(String zoneId:ZoneId.getAvailableZoneIds())
		{
			ZonedDateTime selectedZoneDateTime=ZonedDateTime.now(ZoneId.of(zoneId));
			 System.out.println("Selected time zone is " +  
					 selectedZoneDateTime); 

		 DateTimeFormatter format =  
		     DateTimeFormatter.ofPattern("dd-MM-yyyy HH:mm:ss");  
		   
		 String formatedDateTime = selectedZoneDateTime.format(format);  
		
		 System.out.println("formatted selected time zone "+ 
		                     formatedDateTime); 
			
		}
		
		*/
		
	}

}
